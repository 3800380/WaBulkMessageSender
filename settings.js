const config = {
    NUMBER: '923072380380',
    SESSION_ID: '' //Abhi work nahi kary ga, abhi direct session folder mein creds.json mein session id dycrypt ker ke paste karo......
};

module.exports = config;
